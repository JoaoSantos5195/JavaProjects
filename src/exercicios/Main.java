package exercicios;

import java.util.Scanner;

public class Main {
    public double calculoVolume(double r){
        double vol;
        vol = (4/3 * Math.PI) * Math.pow(r, 3);
        return vol;
    }
}
